# OS
.DS_Store

# Editors
.idea/
.vscode/
*.swp

# Local-only Claude settings
.claude/settings.local.json

# Local build output (release builds are CI-only). Anchored to repo root so it
# doesn't swallow chezmoi-managed scripts under home/dot_local/bin/.
/bin/
/myplace
