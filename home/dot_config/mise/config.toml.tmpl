{{- /* The fleet mise baseline. Body lives in .chezmoitemplates/mise-config.toml so
     the Omarchy twin (conf.d/myplace.toml.tmpl) renders the same tool set; this
     target is .chezmoiignore'd on Omarchy, where config.toml is the distro's. */ -}}
{{ template "mise-config.toml" . -}}
